# axisem-proj

## Setup

Activate the conda environment:

```bash
conda activate instaseis
```

If instaseis is not installed in the environment, install it:

```bash
pip install instaseis
```

## Run

```bash
python instaseis/main.py
```
